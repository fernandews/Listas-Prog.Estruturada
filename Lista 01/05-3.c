// Defina as variáveis a, b e c (tipo de dado) para obter todas as possíveis respostas para
// os valores de ‘c’:
// Suponha que:
// a = 3
// b = a / 2
// c = b + 3.1

// 3. c = 4

#include <stdio.h>

int main() {
    int a = 3;
    int b = a / 2;
    int c = b + 3.1;

   printf("%d", c);

    return 0;
}